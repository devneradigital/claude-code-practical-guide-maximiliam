# MODEL CONTEXT PROTOCOL

Claude Command:
```bash
claude mcp add {mcp server}
```

`/mcp`: Just follow the official documentation's instructions. Also, you can add the mcp globally in your computer, by adding `--scope user`.